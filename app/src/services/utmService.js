// src/services/utmService.js
// PlazaYa — UTM/Install Referrer stub (sem Firebase por enquanto)
// TODO: Adicionar Firebase Analytics depois da aprovação na Play Store

export async function captureInstallReferrer() {
  console.log('[UTM] Install referrer capture skipped (Firebase not configured)');
}
